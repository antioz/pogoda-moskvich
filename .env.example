# Скопируй в .env для локального запуска.
# В GitHub значение лежит в Settings → Secrets and variables → Actions.

# Токен бота от @BotFather. Это единственный секрет — список чатов бот ведёт сам.
BOT_TOKEN=
