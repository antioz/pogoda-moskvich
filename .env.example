# Скопируй в .env для локального запуска (node bot.js).
# В GitHub эти значения кладутся в Settings → Secrets and variables → Actions.

# Токен бота от @BotFather
BOT_TOKEN=

# Канал: @username (если публичный) или числовой chat_id (если приватный)
CHANNEL_ID=@pogoda_dlya_moskvichey
